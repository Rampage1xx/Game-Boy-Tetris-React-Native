interface IChaneGridStatus {
    (patameter: {
        dataGridState: number[][],
        blockMasterArray: number[][],
        blockPositionVertical: number,
        offset: number,
        previousState?: any
    });
}

